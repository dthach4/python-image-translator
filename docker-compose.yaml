version: '3'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    # # Uncomment the following part if you want to protect the API with an API Key
    # environment:
    #   API_KEY: "your_api_key_here"